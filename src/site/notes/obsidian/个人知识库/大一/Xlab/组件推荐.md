---
{"dg-publish":true,"permalink":"/obsidian///xlab//","created":"2024-09-03T11:12:59.946+08:00","updated":"2024-09-08T15:25:05.490+08:00"}
---

# 弹窗提示
## 子组件内全屏显示
- 工具 `Modal`
- 🌰：(后端进阶 RoomEntry)
```tsx
import {Modal} from 'antd' ;

const [noPower, setNoPower] = useState(false);

const handleDeleteRoom = async () => {
	setNoPower(false);  // 重置状态
	const currentUserId = parseInt(localStorage.getItem('currentUserId') || '0', 10);
	if (currentUserId !== creatorId) {
		setNoPower(true);
		return;
	}

	await deleteRoom.mutateAsync({ id: roomId });
};

 {noPower && (  // 如果有错误信息则显示警告框
                <Modal
                    title="删除失败"
                    open={noPower}
                    onOk={() => setNoPower(false)}
                    onCancel={() => setNoPower(false)} 
                 >
                    <p>您没有权限删除该房间 </p>
                 </Modal>   
            )}


```

